export class Utils{static today(){return new Date().toISOString().slice(0,10)}static norm(v=""){return v.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"")}static money(v){return new Intl.NumberFormat("es-EC",{style:"currency",currency:"USD"}).format(Number(v)||0)}static message(el,text,type){el.textContent=text;el.className=`message ${type}`}static status(s){return `<span class="status ${this.norm(s).replaceAll(" ","-")}">${s}</span>`}static fill(select,values,q=""){const old=select.multiple?[...select.selectedOptions].map(o=>o.value):select.value;select.innerHTML="";values.filter(v=>this.norm(v).includes(this.norm(q))).forEach(v=>{const o=new Option(v,v);o.selected=Array.isArray(old)?old.includes(v):old===v;select.add(o)})}static selected(s){return[...s.selectedOptions].map(o=>o.value)}}